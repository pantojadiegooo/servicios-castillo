---
import HeroEngineVisual from './HeroEngineVisual.astro';

interface Props {
  type?: string;
  className?: string;
}

const { type = "", className = "" } = Astro.props;
---

<HeroEngineVisual variant={type === 'castle-impact' ? 'green' : 'blue'} className={className} />
